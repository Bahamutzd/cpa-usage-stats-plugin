# Normalise text files to LF in the repository so CI runners (and any
# checkout on Windows) get consistent line endings. Binary assets keep their
# bytes untouched.
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
*.zip binary